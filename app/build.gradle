apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'androiddebugkey'
            keyPassword 'android'
            storeFile file('/Users/andresgutierrez/.android/debug.keystore')
            storePassword 'android'
        }

        configRelese {
            keyAlias 'myAndroidKey'
            keyPassword 'gutierrez2011'
            storeFile file('/Users/andresgutierrez/Desktop/android.jks')
            storePassword 'gutierrez2011'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "23.0.0 rc3"
    defaultConfig {
        applicationId "com.unal.tuapp.recapp"
        minSdkVersion 15
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.configRelese
        }
        debug {
            signingConfig signingConfigs.config
        }
        
    }
}
repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:cardview-v7:+'
    compile 'com.android.support:appcompat-v7:22.2.0'
    compile 'com.android.support:support-v4:22.2.+'
    compile 'com.android.support:design:22.2.0'
    compile 'com.google.android.gms:play-services:+'
    compile 'de.hdodenhof:circleimageview:1.3.0'
    compile 'com.android.support:recyclerview-v7:+'
    compile 'jp.wasabeef:recyclerview-animators:1.2.1@aar'
    compile 'com.google.maps.android:android-maps-utils:0.4+'
    compile 'com.facebook.android:facebook-android-sdk:4.6.0'
}
